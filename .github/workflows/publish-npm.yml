# SPDX-FileCopyrightText: 2025-2026 Tyrone Ross, Jr <46267523+tyroneross@users.noreply.github.com>
# SPDX-License-Identifier: Apache-2.0
name: Publish @tyroneross/spectra to GitHub Packages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run 'npm publish --dry-run' instead of publishing"
        required: false
        default: "false"

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "24.x"
          registry-url: "https://npm.pkg.github.com"
          scope: "@tyroneross"
          always-auth: true
          package-manager-cache: false

      - name: Verify tag matches package.json version
        if: github.event_name == 'release'
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          TAG="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VERSION" != "$TAG" ]; then
            echo "::error::package.json version ($PKG_VERSION) does not match release tag ($TAG)"
            exit 1
          fi

      - name: Verify plugin manifests are in sync
        run: |
          PKG=$(node -p "require('./package.json').version")
          CLAUDE=$(jq -r .version .claude-plugin/plugin.json)
          CODEX=$(jq -r .version .codex-plugin/plugin.json)
          if [ "$PKG" != "$CLAUDE" ] || [ "$PKG" != "$CODEX" ]; then
            echo "::error::version drift: package.json=$PKG .claude-plugin=$CLAUDE .codex-plugin=$CODEX"
            exit 1
          fi

      - name: Install deps
        run: npm ci --ignore-scripts

      - name: Build (tsc -> dist/)
        run: npm run build

      - name: Prepare GitHub Packages metadata
        run: |
          npm pkg set publishConfig.registry=https://npm.pkg.github.com
          npm pkg delete publishConfig.provenance

      - name: Check if version already exists
        id: existing
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PKG=$(node -p "require('./package.json').name")
          VER=$(node -p "require('./package.json').version")
          if npm view "$PKG@$VER" version --registry https://npm.pkg.github.com >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "GitHub Packages already has $PKG@$VER"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish (dry-run)
        if: steps.existing.outputs.exists != 'true' && github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm publish --dry-run --registry https://npm.pkg.github.com

      - name: Publish
        if: steps.existing.outputs.exists != 'true' && (github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run != 'true'))
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm publish --registry https://npm.pkg.github.com
