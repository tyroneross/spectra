# Handoff: M3.G2 — implementer briefs (rev 2, critic-hardened)

Source of truth: `docs/plans/m3-g2-plan.md` (rev 2). Global rules bind every dispatch: rally claim first; never commit without authority; pins are law (ADR-07 lifts ONLY SessionOps.swift/SessionStore.swift, single owner S1 — PermissionOps.swift, LibraryOps.swift, SpectraViewModel.swift, daemon-runner.ts stay never-edit); temp `HOME`/`SPECTRA_HOME` for anything that could write captures (`.spectra/` is PROTECTED real data); never trigger the login keychain (Chrome: `--password-store=basic --use-mock-keychain`); never `rm` a path you didn't create; ✅/⚠️/❓ claims name HOW verified. TS backend `src/` is read-only reference for every G2 agent. **The 4 allowlist-importer test files (conformance.test.ts, capability-gate, external-mode.test.ts, corpus/corpus.test.ts) are OUT OF BOUNDS for everyone — they must need zero edits (SG-5).**

Build/verify loop from a fresh shell: `swiftc macos/Spectra/DaemonCore/*.swift -o /tmp/spectra-daemon-g2` then `npx tsx macos/Spectra/DaemonCore/verify-g2-suite.ts` (S7 delivers it; until then use `verify-g1-suite.ts` as the non-regression floor — 31/31 must stay green in every chain).

## W0 — interface freeze (FIRST, serial) — the whole cross-agent symbol surface (SG-3)
Deliver `DriverProtocol.swift` freezing, in code + doc-comment contracts:
1. **Driver protocol** mirroring `src/core/types.ts` — signatures AND **behavioral contracts** (PC-4): act on unknown/stale element returns `success:false` + fresh snapshot, never throws (src/native/driver.ts:88-96 behavior); snapshot element ids sequential `e1..eN` per snapshot; screenshot returns PNG data; connect validates target. FakeDriver (S1) and NativeDriver (S2) both conform to the WRITTEN semantics.
2. **DriverRegistry** — concrete class (get/set/remove by sessionId, single-writer). Declared here; S1 populates; S3/S4 consume. This is the most-shared symbol — the exact G1 drift class.
3. **DaemonContext extension contract** — the exact field names/types S6 adds to DaemonContext (HandlerRegistry.swift): `driverRegistry`, `recordingOwnership`, sessionStore access. Written here; S6 lands it as its first slice.
4. **SessionStore public write surface** for S3/S4: `addStep` / `addDecision` / `addArtifact` / `setRecordingStatus` + **public `sessionDir(_:)`** (today `sessionDirLocked` is private, SessionStore.swift:590 — S1 exposes).
5. **The 5 register-hook signatures** S6 wires: `registerConnectOps` / `registerAxOps` / `registerStepOps` / `registerCaptureRecordingOps` / `registerTerminalOps` — exact names frozen here.
6. **NativeDriver factory** `makeNativeDriver(appName:) throws -> Driver` (S1 calls, S2 implements) + **RecordingOwnership** protocol `ownsRecording(_:) -> Bool` (S4 implements, S6 consumes).
Plus the D-03 v2 config schema doc-comment, the mapping-doc "(14 ops)"→16 fix, and the rally handoff to Codex (M4's CdpDriver implements THIS frozen protocol; post-freeze changes need an Advisor ruling).

## TCC spike (PC-2 — runs once S2's BridgeClient + S7's probe runner exist; gates S2/S4 acceptance)
T-25 step 1 only, **under the PRODUCTION launch context (the launchd plist, NOT a dev shell — TCC attribution keys on parent chain / responsible process / signature; dev-shell green does not transfer)**: the daemon-core spawns the AX helper (`snapshot` on TestApp/Finder) + the screen-recording preflight; record granted/denied to `.build-loop/flip-evidence/gate-g2-tcc-spike.txt`. If attribution fails → STOP, surface to the Advisor (ADR-05 fallback rung: stable signing identity + user re-grant). S2/S4 success-path acceptance is CONDITIONAL on this evidence.

## S1 — session-driver-core
When implementing F-10, read ADR-06 + ADR-07 and satisfy T-20/T-24. Own: `ConnectOps.swift`, `FakeDriver.swift`, `SessionStore.swift`, `SessionOps.swift`. createSession: target detection parity with `src/mcp/context.ts` detectPlatform (web URL → return the tunnel-delegate decision to the Router; macos appName → NativeDriver via W0's factory; `fake:` → FakeDriver, ONLY under `SPECTRA_CONFORMANCE_SEED=1`; sim → tunnel, ND-3). NO ownership bookkeeping — store-presence IS the routing signal (ADR-04 rev 2); your only obligation is that the session is in SessionStore before the createSession response is written. `record: true` macos sessions skip the AX snapshot (parity with connect.ts:105 — Screen-Recording-only grant path). Session run model (D-04): steps, decisions, artifacts, recording status — shape parity with what getSession/getRun already serve; expose the W0-frozen public write surface incl. public `sessionDir(_:)`. FakeDriver mirrors `tests/conformance/lib/fakes.ts` EXACTLY (element tree, act semantics, fixed PNG, its Date.now() volatility is pre-ruled) AND conforms to W0's behavioral contracts. Param validation + CapabilityPolicy registration for your ops (F-18).

## S2 — ax-engine (now owns the snapshot + act handlers — SG-4)
When implementing F-11, read ADR-05 and satisfy T-20 (taxonomy arms) + T-21 + T-25 (steps 2–4). Own: `BridgeClient.swift`, `NativeDriver.swift`, `RoleNormalize.swift`, `SnapshotSerialize.swift`, `SnapshotOps.swift`, `ActOps.swift`, `ComputerUseOps.swift`. BridgeClient: stdio line-JSON RPC to the EXISTING spectra-native helper — **the contract spec is `src/native/bridge.ts`** (ping handshake, snapshot/press/setValue framing, 5s request timeout, 30s heartbeat; helper side `native/swift/main.swift`+`AXBridge.swift`); the helper is NOT yours to edit — a needed helper change is a FINDING. NativeDriver: implements W0's factory + behavioral contracts; element-id ↔ AX-path map; role normalization parity with `src/core/normalize.ts`; serializeSnapshot parity with `src/core/serialize.ts` (the conformance seeding regex `[e42] button "…"` depends on that exact line format — fixture-context.ts:87). snapshot/act op handlers: pure driver passthrough + serialization per `src/mcp/tools/{snapshot,act}.ts`. computerUse: sessionless AX target (pid/app), instance-cache per target (core-impl.ts:159 rationale), AxPermissionError → `permission_denied` 403 with the Settings hint (code + status byte-parity; message is masked class).

## S3 — step-intelligence-engine
When implementing F-12, satisfy T-20/T-21 (+ T-25 steps 8–9 exercise your ops natively). Own: `Resolve.swift`, `Actions.swift`, `StepOps.swift`, `Intelligence.swift`, `AnalyzeOps.swift`, `DiscoverOps.swift`. Port `src/core/resolve.ts` (intent matcher, confidence, candidates), `src/core/actions.ts` (selectActionForElement), `src/intelligence/{importance,states,framing}.ts` — DETERMINISTIC heuristics, no LLM anywhere (llmStep executes a client-built ActionPlan[]; walkthrough loops handleStep). **Port the TS test vectors as XCTest cases** — V-B's structural floor catches drift, your unit vectors catch it earlier. step auto-execute threshold (confidence > 0.9, no candidates), decision records into the session (S1's frozen write surface), walkthrough completed-only-if-autoExecuted semantics, observe = snapshot ⊕ session ⊕ recording ⊕ optional analyze (core-impl.ts:292). Your duration/snapshot fields sit inside the pre-ruled mask classes — do NOT rely on that: emit them correctly typed (number ≥ 0, non-empty strings) or the typed guards FAIL you.

## S4 — media-recording
When implementing F-13/F-14, read ADR-05 and satisfy T-20 (taxonomy arms) + T-21 + T-25 (steps 5–6). Own: `CaptureOps.swift`, `RecordingOps.swift`, `FfmpegProbe.swift`. screenshot: modes full/element/region/auto per `src/mcp/tools/capture.ts`; PNG decode/crop/encode via CoreGraphics (headless-safe; the cross-encoder byte difference is pre-ruled — the artifact probe compares decodability + dimensions, not bytes); artifact write into S1's public sessionDir; `clean` is CDP-only → native/fake = `cleanApplied: false`, never an error. Recording: orchestration parity with core-impl.ts — macos+appName guard (400 recording_failed), per-session conflict (409), window resolution via the already-native listWindows provider, **recording helper RPC per `src/daemon/core-impl.ts` NativeRecordingProcess (startRecording/stopRecording/quit; 15s/45s/1s timeouts; helper side `native/swift/MediaCapture.swift`)**, cursor-sampler spawn + skip-warning semantics, keep-awake analog, black-frame guard + probeVideo via SHELLED ffmpeg (M5 rule: no AVFoundation), artifact + recording-status writes, stopRecording union (Completed | AlreadyStopped — the oracle bites on `{}`). getRecording: composite registry hit → record; active single-window → synthesized shape; else 404 (core-impl.ts:582). Registries single-writer; implement W0's `RecordingOwnership`.

## S5 — terminal
When implementing F-15, satisfy T-20/T-21 (both FULLY headless — you have no hardware excuse). Own: `TerminalOps.swift`, `CastParser.swift`. recordTerminal: openpty + child exec, asciinema-cast v2 writer, timeline events `{time, source, event}` (structural class, rev 3.3 comparator), optional watch_files, maxDuration; `outputSize`/`lines` must be deterministic for the fixed V-B command — a flake there is a FINDING against you, not a mask. replayTerminal: parse/search/extractCommands/formatCastSummary parity with `src/terminal/parser.ts` — fixed committed fixture cast, FULLY byte-compared (rev 3.3 exclusion stands: your summary string must match TS byte-for-byte).

## S6 — router-v2-dispatch-plane (SG-1: your surface is Router + SocketServer + ProxyClient + wiring)
When implementing F-16, read ADR-04 (rev-2 store-presence addendum) and satisfy T-23/T-24/T-27. Own: `Router.swift`, `SocketServer.swift`, `ProxyClient.swift`, `HandlerRegistry.swift`, `main.swift`.
- **Router v2 loader** — boot-refusing invariants (each with a red test): unknown version; session-scoped op in plain `native:[]`; affinity/merge/fanout op with no registered handler; list overlap; malformed JSON. v1 configs stay valid verbatim (rollback target). `route(for:)` becomes **params-aware for affinity ops only** — native/proxy buckets keep the parse-free byte-tunnel fast path.
- **SocketServer** — pre-route envelope decode for affinity ops (decode failure = deterministic `bad_request`, NEVER a silent tunnel); new `merge`/`fanout` arms in the dispatch switch (today `.native`/`.proxy` only, :156-159); native-createSession store insertion in `dispatchNative`'s result path (session in store BEFORE the response is written); `origin:` (store-hit/miss) in `logLine` (:248).
- **ProxyClient** — a body-CAPTURING tunnel variant (today `tunnel()` returns only status, :104) used ONLY by merge/fanout/dual-run (store-presence killed the createSession-response-parse need); prove the captured leg byte-faithful (T-27 arm). Widen `DualRunRecorder` (:369) to the new dual-run-eligible read ops.
- **Store-presence dispatch:** SessionStore hit → native; recording ops via W0's `RecordingOwnership`; miss → tunnel. listSessions merge: Swift-owned first then backend, each sorted (createdAt, id) — T-27 runs ×5 for order stability. closeAllSessions: native close + tunneled close, aggregate counts, both sides proven closed.
- DaemonContext extension exactly per W0 (c); wire the 5 W0-frozen register hooks — only YOU edit HandlerRegistry/SocketServer/ProxyClient/main.

## S7 — gate-harness (SG-2/SG-5: harness compatibility is a hard constraint)
When implementing F-17, satisfy T-20/T-21/T-22/T-25 tooling + the Q-criteria audits. Own: `tests/conformance/lib/external-mode.ts`, `tests/conformance/lib/fixture-context.ts`, `tests/conformance/lib/front-door.ts`, `tests/conformance/lib/payload-generator.ts`, `verify-g2-suite.ts`, `verify-g2-ondevice.{sh,ts}`.
- **external-mode:** KEEP the `SWIFT_G1_VERIFIABLE_OPS` export name + derived skip-set (pin P4). Widen via a milestone env gate (`SPECTRA_CONFORMANCE_MILESTONE=g2` [ASSUMED name], widen-only, module-load read — the proxy-fidelity pattern at :91) adding the 13 headless G2 ops. Env unset = byte-identical G1 behavior. **The 4 importer test files need ZERO edits — verify with git diff (Q-criterion).**
- **front-door.ts:** APPEND-ONLY `FrontDoorHarnessOptions` additions (`routingConfig` override + `extraEnv`) — the hardcoded v1 `PRODUCTION_ROUTING_CONFIG` (:62/:147) STAYS the default so Gate-B's G1 regression is byte-identical.
- **payload-generator.ts:** seed-gated `fake:` branch in the `'target'` case (:79-87) so the generic suite exercises Swift's NATIVE createSession (today it always emits the web fixture URL → tunneled).
- **fixture-context.ts:** route capture-op fixtures at the fake-driver seeded session under the seed-session path.
- **verify-g2-suite.ts:** V-A run + V-B differential — rev-3.5 comparator + the pre-ruled G2 map (generated-id cross-leg normalization with referential-consistency guard; `generated-image-content` artifact probe (decode + dimensions, not bytes); structural floor incl. bounds/type/value-presence checks; CONDITIONAL `stateful-read-timestamp` class that auto-applies on first observed flake with a ledger entry). Anything outside the map = stop + Advisor ruling. Re-derive the recordLlmUsage.entries mask (no weaker than rev 3.2). Error-taxonomy arms for startRecording/stopRecording/computerUse (code + status byte-compared).
- **verify-g2-ondevice:** the 9-step script (step 1 = TCC probe under the launchd production context; steps 8–9 = observe/analyze + step/llmStep vs TestApp) + evidence file + classed excluded-set for host-unavailable steps.

## Acceptance sequence (Opus schedules)
Wave-1 fan-out → TCC spike (production context) as soon as S2+S7 land → Opus integration → V-A green → V-B 3 consecutive fully-green chains (each incl. the G1 31/31 arm + B-e2e) → V-C 9-step with the user present (evidence file) → Fable group verdict → C10 flip commit → Gate E2 (live soak + dual-run rows + rogue-daemon detector + v2→v1 rollback drill <2 min) — pauses for the user.
