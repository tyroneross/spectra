---
name: sessions
description: List and manage Spectra sessions
---

Show all active and stored Spectra sessions.

Use `spectra_session` with action "list". Display results in a table showing:
- Session ID
- Name
- Platform
- Steps count
- Created time

If the user wants to close a session, use `spectra_session` with action "close".
