---
name: capture
description: Quick screenshot or video of current session
---

Capture the current state of the active Spectra session.

Use `spectra_capture` with type "screenshot". If no session is active, inform the user to connect first with `/spectra:connect`.
