<!-- SPDX-FileCopyrightText: 2025-2026 Tyrone Ross, Jr <46267523+tyroneross@users.noreply.github.com> | SPDX-License-Identifier: Apache-2.0 -->

# Coordination — UI best practices + C7.a benchmark validity (2026-05-24)

**Date:** 2026-05-24
**Session:** claude_code (P1 owner — macOS SwiftUI); codex (P2 + P3 owner — web-ui + benchmark validity)
**Status:** active — archived per closeout when all three pieces land
**Predecessor:** none (first coordinated run on spectra)

## Scope

Update Spectra UI to best practices across both surfaces, AND fix C7.a walkthrough-bench measurement validity before any live DOE run. Three pieces, two hosts, one rally point.

**Pieces:**

- **P1-macos-ui** — macOS SwiftUI views (RepoPicker, Settings, AccessibilityPanel, MenuBarPopover, +ViewModel) brought to Apple HIG + Calm Precision + a11y + action-button-state + empty/error/loading + ≤3 user journeys. **Owner: claude_code** (already dispatched in worktree, agent `a56c57ac939850950`).
- **P2-web-ui** — Next.js `web-ui/` brought to the same best-practice bar: Calm Precision visual hierarchy, a11y (WCAG 4.5:1, 44px mobile / 24px desktop), action-button-state, empty/error/loading, ≤3 user journeys. **Owner: codex.**
- **P3-bench-validity** — Fix C7.a walkthrough-bench measurement defects before any live DOE: (a) F1 snapshot-mode actually exercised at `runner.ts:289` + `WalkthroughPlanner.swift:167`; (b) URL field added to `spectra_snapshot` so `tp-nav-to-trips` is scorable; (c) F4 retry semantics match design language (re-snapshot + replan on step failure); (d) success criterion requires explicit `done` or predicate match; (e) split `llmLatencyMs` and `executorLatencyMs` in `runs.jsonl`. **Owner: codex.**

**Out of scope (this run):**

- Live C7.a DOE execution (gated behind P3 landing + a one-cell smoke).
- C7.b video-bench runner implementation (`.build-loop/experiments/video-bench/runner.ts.PLAN.md` stays a plan).
- DMG codesign / packaging fixes.
- New features. Behavior changes only where required to honor the above.

## Operating Rule (binding — inherited from references/coordination-rules.md)

A piece does not advance past `verification-pending` until the peer verifier posts one of:

- `PASS` — acceptance criteria verified.
- `VARIANCE` that has been resolved (fix landed, documented non-acceptance, OR escalated to user).
- Explicit user override.

A `VARIANCE` left unresolved blocks the next piece. `BLOCKED` requires the producing peer to supply missing evidence before proceeding.

Per-run amendments: none.

## Coordination Protocol (binding — inherited)

- **All Rally Point writes** go through `scripts/rally_point/post.py` `post()` helper.
- **Cheap detection at every step boundary**: `python3 .build-loop/.../coordination_status.py --workdir . --session-id <id> --coordination-file .build-loop/coordination/ui-best-practices-2026-05-24-2026-05-24.md --json`. `--coordination-file` REQUIRED.
- **Verifier feedback entry format** (append-only at bottom):

  ```md
  ### YYYY-MM-DD HH:MM TZ — <verifier> <VERDICT>
  **Step:** <piece id>
  **Verdict:** PASS | VARIANCE | BLOCKED
  **Evidence:** <file:line or command result>
  **Impact:** <why this matters>
  **Requested action:** <fix / explain / user decision>
  ```

## MECE Ownership Packets per Piece

### P1-macos-ui — macOS SwiftUI best-practices pass

- **Owns** (claude_code):
  - `macos/Spectra/Views/RepoPicker.swift`
  - `macos/Spectra/Views/SettingsView.swift`
  - `macos/Spectra/Views/AccessibilityPanel.swift`
  - `macos/Spectra/Views/MenuBarPopover.swift`
  - `macos/Spectra/SpectraApp.swift`
  - `macos/Spectra/ViewModels/SpectraViewModel.swift`
  - `docs/UX_JOURNEYS.md` (new)
- **Does not own**: `web-ui/**`, `src/**`, `.build-loop/experiments/**`, `macos/Spectra/LLM/**`, `macos/Spectra/Net/**`, `macos/Spectra/Daemon/**`, `macos/Spectra/Storage/**`.
- **Interface contract**: `xcodebuild test -scheme Spectra` stays 25/25 green. Public Swift API of `WalkthroughPlanner` / `DaemonClient` / `KeychainStore` unchanged. Any new view modifiers exposed as internal-only.
- **Integration checkpoint**: codex verifies by reading `docs/UX_JOURNEYS.md` + the modified Views + running `xcodebuild test -scheme Spectra` + spot-checking that no symbol from "does not own" set changed.

### P2-web-ui — Next.js web-ui best-practices pass

- **Owns** (codex):
  - `web-ui/app/**`
  - `web-ui/components/**`
  - `web-ui/lib/**` (if UI utilities only)
  - `web-ui/__tests__/**` (UI tests)
  - `docs/WEB_UX_JOURNEYS.md` (new)
- **Does not own**: `macos/**`, `src/**`, `.build-loop/experiments/**`, `web-ui/lib/api/**` (daemon transport stays as-is unless P3 schema change requires a sympathetic update).
- **Interface contract**: `cd web-ui && npm test` stays green (current count to be recorded by codex on first run). `cd web-ui && npx tsc --noEmit` clean. No new runtime deps unless justified inline.
- **Integration checkpoint**: claude_code verifies by reading `docs/WEB_UX_JOURNEYS.md` + running an IBR scan (`/ibr:scan` against `http://localhost:3000` after `cd web-ui && npm run dev`) + checking the per-view a11y / contrast / touch-target outputs.

### P3-bench-validity — C7.a walkthrough-bench measurement-correctness fix

- **Owns** (codex):
  - `.build-loop/experiments/walkthrough-bench/runner.ts`
  - `.build-loop/experiments/walkthrough-bench/analyze.ts`
  - `.build-loop/experiments/walkthrough-bench/tasks.yaml` (predicate-only edits if needed)
  - `src/mcp/tools/snapshot.ts` (add URL/currentLocation field)
  - `src/core/serialize.ts` (if URL surfacing requires it)
  - `macos/Spectra/LLM/WalkthroughPlanner.swift` (snapshot-mode + retry semantics + success criterion)
  - `tests/**` (new tests: snapshot-mode propagation, URL predicate scoring, retry-on-step-failure, split latency)
  - `docs/HANDOFF.md` (one-cell smoke instruction)
- **Does not own**: `macos/Spectra/Views/**` (P1 territory), `web-ui/**` (P2 territory).
- **Interface contract**: `npm test` (currently 479/479) stays green; `tsc --noEmit` clean; `xcodebuild test -scheme Spectra` stays 25/25 green. The `spectra_snapshot` tool gains a NEW field — `url?: string` — and is backwards-compatible (older clients ignore it). `runs.jsonl` schema gains two NEW fields (`llmLatencyMs`, `executorLatencyMs`); analyzer reads both, falls back to legacy `latencyMs` if absent.
- **Integration checkpoint**: claude_code verifies by reading the diff + running both test suites + grep-confirming snapshot-mode is actually passed at the two cited lines + reading `docs/HANDOFF.md` for the one-cell smoke command.

## Step status (live)

| # | Piece | Owner | Status | Pending verifier check |
|---|---|---|---|---|
| P1-macos-ui | macOS SwiftUI best-practices | claude_code | ✅ ready-to-merge — `worktree-agent-a56c57ac939850950` ff onto post-P2/P3 main; codex verification waived per user directive (P1 isolated to macOS surface, no overlap with P2/P3) | n/a — user override |
| P2-web-ui | Next.js web-ui best-practices | codex | ✅ PASS (verifier: claude_code) — committed | yes — claude_code |
| P3-bench-validity | C7.a walkthrough-bench validity fix | codex | ✅ PASS (verifier: claude_code) — committed | yes — claude_code |

**Status legend:** `⏸️ awaiting dispatch` → `🏃 executing` → `✅ executed; verification-pending` → `✅ PASS (verifier)` → `done`. Variances surface as `⚠️ VARIANCE — <one-line>` until resolved.

## Acceptance criteria per piece (for verifier checking)

### P1-macos-ui

- Every action button (`Done`, `Save`, `Apply`, `Add`, `Submit`, `Connect`, etc.) has distinct enabled vs disabled visual treatment — grep for `Button(` across owned files; eyeball each.
- Every interactive control has an `.accessibilityLabel`; `.accessibilityHint` where action is non-obvious — grep + ibr:native-testing scan.
- Every async-dependent view handles empty / error / loading explicitly — read each view.
- 3 user journeys documented in `docs/UX_JOURNEYS.md` with screenshots from ibr:native-testing.
- No user-facing jargon (`AX`, `MCP`, `JSON-RPC`, `daemon`, `stdio`, `IPC`) in view code copy — grep across owned files.
- `xcodebuild test -scheme Spectra` 25/25 ✅.
- `Spectra.app` + `Spectra.dmg` refreshed at repo root (ad-hoc OK).

### P2-web-ui

- Calm Precision: single border around related items, never per-item; content ≥ 70% of viewport; text hierarchy (14–16px / 12–14px / 11–12px); nav selected = text weight + 2px bottom border, no pill.
- WCAG 4.5:1 contrast; 44px mobile / 24px desktop hit targets — IBR scan confirms.
- Action button states (enabled/disabled) on every form.
- Empty / error / loading on every fetch surface.
- 3 user journeys documented in `docs/WEB_UX_JOURNEYS.md`.
- `cd web-ui && npm test` green; `npx tsc --noEmit` clean.

### P3-bench-validity

- `.build-loop/experiments/walkthrough-bench/runner.ts:~289` passes `screenshot: cell.snapshot === 'axPlusScreenshot'` to `spectra_snapshot` — grep confirms.
- `macos/Spectra/LLM/WalkthroughPlanner.swift:~167` honors `config.snapshot` — grep confirms.
- `src/mcp/tools/snapshot.ts` `SnapshotResult` has `url?: string` (or `currentLocation`); set on web driver — read code.
- `evaluatePredicate` resolves `url_matches` against new field — new unit test asserts pass on a simulated `https://x/trips` snapshot.
- `oneRetryResnapshot`: on simulated executor-step failure, harness re-snapshots and replans exactly once — new test.
- App walkthrough success requires `done` action OR predicate match — new test asserts no-`done`-no-predicate run returns failure.
- `runs.jsonl` has `llmLatencyMs` + `executorLatencyMs` (legacy `latencyMs` may remain) — analyzer reads both.
- `npm test` ≥ 479 + new tests, all green.
- `tsc --noEmit` clean.
- `xcodebuild test -scheme Spectra` 25/25 green.
- `docs/HANDOFF.md` contains the exact one-cell smoke command: `ANTHROPIC_API_KEY=… npm run bench:walkthrough -- --cells=00`.

## Anti-checklist

- No new coordination-file format — this file stays the only one.
- No new presence mechanism — Rally Point only.
- No live DOE run during this rally.
- No silent scope creep — surface as `VARIANCE` first.
- No skipping `npm test` / `tsc --noEmit` / `xcodebuild test` before posting PASS.

---

## Verifier feedback log

<!-- Append-only. Verifier posts entries below per the format in Coordination Protocol. -->

### 2026-05-24 11:30 PDT — claude_code PASS (verifier on P2)
**Step:** P2-web-ui
**Verdict:** PASS
**Evidence:**
- `cd web-ui && npm test` → 17/17 green
- `cd web-ui && npx tsc --noEmit` → clean
- App-router state files exist + render: `web-ui/app/error.tsx`, `loading.tsx`, `not-found.tsx`
- `docs/WEB_UX_JOURNEYS.md` documents 3 journeys (Review / Inspect / Export) with explicit empty/loading/error/disabled-state expectations per surface
- 13 web-ui components modified for Calm Precision hierarchy + action button states + a11y
- `web-ui/package.json` adds `test` + `typecheck` scripts (no new runtime deps)
**Impact:** P2 acceptance criteria satisfied. Ready to commit.
**Requested action:** Commit P2 + proceed.

### 2026-05-24 11:30 PDT — claude_code PASS (verifier on P3)
**Step:** P3-bench-validity
**Verdict:** PASS
**Evidence:**
- `runner.ts:294` passes `screenshot: cell.snapshot === 'axPlusScreenshot'` ✓
- `snapshot.ts:12,28` adds `url?: string` field, set from driver `snap.url` ✓
- `score.ts:33-35` URL predicate resolves `url_matches` against snapshot URL ✓
- `WalkthroughPlanner.swift:199-200` honors `config.snapshot == .axPlusScreenshot` ✓
- `WalkthroughPlanner.swift:43-45` `WalkthroughSuccessPolicy.evaluate(done:predicateMatched:lastError:)` requires `done != nil || predicateMatched` ✓
- `runner.ts:415-419` `shouldRetryStepFailure` re-loops (re-snapshot + replan) on step failure when `oneRetryResnapshot` ✓
- `runner.ts:89-90` + `analyze.ts:32-33` split `llmLatencyMs[]` + `executorLatencyMs[]`; analyzer falls back to legacy `latencyMs` ✓
- `tests/experiments/walkthrough-runner.test.ts` validates snapshot-mode propagation ✓
- `docs/HANDOFF.md` documents the one-cell smoke command ✓
- `npm test`: 484/484 green (was 479; +5 new tests) | `tsc --noEmit`: clean | `xcodebuild test`: 27/27 green (was 25; +2 new Swift tests)
- **Scope-note accepted:** `AnthropicClient.swift` Content/Block image-block support necessary for screenshot-mode end-to-end fidelity — without it, `screenshot: true` would be a no-op at the HTTP boundary. Per "always the durable fix" preference, this is the correct attack-the-root-cause patch.
**Impact:** P3 acceptance criteria satisfied + the one-cell smoke is now meaningful (image blocks reach the API). Ready to commit.
**Requested action:** Commit P3 + proceed.

### 2026-05-24 11:09 PDT — claude_code SELF-REPORT (P1 — verification waived per user directive)
**Step:** P1-macos-ui
**Verdict:** SELF-REPORT — implementation complete; codex verification pending
**Evidence:**
- Commit: `492f0f5` (1 ahead of main `09601be`); worktree `.claude/worktrees/agent-a56c57ac939850950/`
- `xcodebuild test -scheme Spectra` → 25/25 green
- New files: `DesignTokens.swift`, `ActionButtonStyle.swift`, `LoadableState.swift`, `Copy.swift`, `docs/UX_JOURNEYS.md`
- Modified: `SpectraApp.swift`, `SpectraViewModel.swift`, 4 view files
- A11y labels 1 → 31; hints 0 → 16; semantic colors only; Dynamic Type throughout
- No user-facing jargon (AX/MCP/JSON-RPC/stdio/IPC) in displayed strings
- Action buttons use `.spectraProminent` / `.spectraStandard` styles that honor `\.isEnabled`
- All 5 catch sites route through `RecoveryError.from()` for graceful failure
- `Spectra.app` + `Spectra.dmg` refreshed in worktree (1.04 MB DMG, ad-hoc signed)
**Impact:** P1 ready for codex verification. Files owned by P1 are confined to `macos/Spectra/**`, `docs/UX_JOURNEYS.md` — no overlap with P2 (`web-ui/**`) or P3 (`src/mcp/tools/snapshot.ts`, `.build-loop/experiments/walkthrough-bench/**`, `macos/Spectra/LLM/WalkthroughPlanner.swift`).
**Requested action:** codex to verify per acceptance criteria; on PASS, claude_code merges ff-only into main.
