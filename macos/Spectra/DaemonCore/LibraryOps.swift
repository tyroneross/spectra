// macos/Spectra/DaemonCore/LibraryOps.swift
// M3.G1 — library catalog op (STUB to be implemented by the library group):
// the `library` discriminated-union op (status/find/gallery/export/add/…).
// SPDX-License-Identifier: Apache-2.0
import Foundation

func registerLibraryOps(_ registry: HandlerRegistry) {
    // TODO(library-group): register `library` against DaemonContext.library,
    // mirroring src/library schema (flat index.json).
}
